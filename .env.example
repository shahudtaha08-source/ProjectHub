# Optional overrides. If this file is absent (or a value is left blank),
# main.py falls back to its built-in default: D:\ProjectHub\files
# Copy this file to ".env" and edit as needed - .env itself is never
# required for the script to run.

PROJECT_HUB=D:\ProjectHub\files
PROJECTS_ROOT=
